export type TypeBrushPoint = {
  pattern: HTMLImageElement | HTMLCanvasElement,
  width: number,
  height: number,
  maxSize: number,
  minSize: number,
}

export type TypeBrushOptions = {
  src: string
}